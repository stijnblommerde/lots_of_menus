{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Rename Restaurant</h1><hr/>
        {{ form_start(myForm) }}
        {{ form_widget(myForm) }}
        <input type="submit" value="Rename" class="btn btn-default" />
        {{ form_end(myForm) }}
    </div>
{% endblock %}