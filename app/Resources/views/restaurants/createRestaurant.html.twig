{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Create Restaurant</h1><hr/>
        {{ form_start(myForm) }}
        {{ form_widget(myForm) }}
        <input type="submit" value="Create" class="btn btn-default" />
        {{ form_end(myForm) }}
    </div>
{% endblock %}
