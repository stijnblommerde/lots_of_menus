{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Edit Menu Item</h1><hr/>
        {{ form_start(menu_form) }}
        {{ form_widget(menu_form) }}
        <input type="submit" value="Edit" class="btn btn-default" />
        {{ form_end(menu_form) }}
    </div>
{% endblock %}