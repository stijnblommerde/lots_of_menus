{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Create Menu Item</h1><hr/>
        {{ form_start(menu_form) }}
        {{ form_widget(menu_form) }}
        <input type="submit" value="Create" class="btn btn-default" />
        {{ form_end(menu_form) }}
    </div>
{% endblock %}